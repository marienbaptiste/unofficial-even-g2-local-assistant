# Even G2 Voice Service — Whisper STT
numpy==1.26.4
torch>=2.6.0,<2.7.0
faster-whisper>=1.1.0
# WhisperLive installed separately in Dockerfile.base (--no-deps to avoid numpy conflict)
# Server
fastapi==0.115.6
uvicorn[standard]==0.34.0
websockets==14.2
python-multipart==0.0.20
requests
