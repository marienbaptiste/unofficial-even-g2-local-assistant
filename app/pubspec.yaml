name: g2_test_app
description: Desktop test app for Even G2 SDK (Windows, macOS, Linux).
publish_to: 'none'

version: 0.4.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  even_g2_sdk:
    path: ../sdk
  http: ^1.2.0
  web_socket_channel: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
